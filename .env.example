# Backend

# Generate with `openssl rand -base64 48`
ADMIN_JWT_SECRET=
ADMIN_SECRET_TOKEN=

# Public URL that will host generated launcher files
DOWNLOAD_SERVER_BASE=https://downloads.example.com/data
# Optional override (defaults to ${DOWNLOAD_SERVER_BASE}/assets/objects)
RESOURCES_URL_BASE=

# Controls whether launcher assets are always served from DOWNLOAD_SERVER_BASE
# Accepts true/false
REPLACE_DOWNLOAD_URLS=false

# Optional hooks executed by instance_builder before/after all versions
EXEC_BEFORE_ALL=
EXEC_AFTER_ALL=

# =====================
# Frontend

# Frontend build-time URL base
VITE_API_BASE_URL=/api/v1
VITE_GITHUB_URL=https://github.com/Petr1Furious/potato_launcher

# =====================
# Common

LAUNCHER_NAME=Potato Launcher

PUID=1000
PGID=1000
