version: '3.8'

services:
  backend:
    image: "my_backend"
    #build:
    #  context: .
    #  dockerfile: backend.dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./out:/backend/out # тут вопросики к структуре папок
      # сделать волюмы для остальных данных, которые не должны удаляться после перезапуска
      #- ./out:/backend/out